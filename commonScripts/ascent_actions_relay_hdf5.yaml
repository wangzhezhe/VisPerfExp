#An example of using a relay extract to save a subset of the data.

- 
  action: "add_extracts"
  extracts: 
    e1: 
      type: "relay"
      params: 
        path: "testoutput"
        protocol: "blueprint/mesh/hdf5"
- 
  action: "execute"
